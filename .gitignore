__pycache__/
*.pyc

# قاعدة البيانات فيها بيانات المكتب الحقيقية (عملاء/فواتير...) — ما تنحفظ بالمرجع
office_system.db

# مستندات مولّدة + صور خلفية مخزّنة مؤقتاً (cache) — تُنشأ من جديد تلقائياً عند الحاجة
output/

# إعدادات المكتب المحلية (آخر Agence/Guichet/Caisse/Guichetier) ومسودة
# العمل الجاري بشاشة CD — حالة خاصة بجهاز/جلسة المستخدم، مو جزء من الكود
cd_settings.json
cd_draft.json
cd_ui_prefs.json

# نفس المبدأ: تفضيلات محلية بحتة (آخر مسار فتحه المستخدم بالشريط
# الجانبي، إعدادات/تاريخ النسخ الاحتياطي) — خاصة بجهاز المستخدم، مو كود
file_explorer_prefs.json
backup_settings.json

# قاعدة بيانات قديمة فارغة غير مستخدمة (بقايا من إعادة تسمية سابقة)
officemanager.db
