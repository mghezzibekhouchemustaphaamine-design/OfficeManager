# الجزء الأساسي من البرنامج (العملاء، الفواتير، المهام، المستندات) يعتمد
# فقط على مكتبات بايثون القياسية (tkinter, sqlite3, csv) — لا حاجة لأي
# تثبيت لتشغيله.
#
# خدمة CD (Change Devise) تحتاج المكتبات التالية:
python-docx   # يبني ملف الـ.docx النهائي مباشرة (بدون فتح Word إطلاقاً)
Pillow        # يرسم صورة النموذج الفاضي حياً على الشاشة (بدون Word أيضاً)
reportlab     # يبني نسخة PDF حقيقية (نص متجهي حاد) من نفس البيانات

# ملاحظة: كان يُستخدم سابقاً pywin32 (لفتح Word عبر COM) وPyMuPDF (لتحويل
# PDF إلى صورة) لرسم خلفية النموذج — تم الاستغناء عنهما بالكامل، فما عاد
# البرنامج يحتاج Word مثبّتاً على الجهاز إطلاقاً لأي جزء من عمله.
#
# إن أردت لاحقاً إضافة تصدير الفواتير كـ PDF يمكنك تثبيت:
# reportlab
