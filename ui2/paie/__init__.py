"""شاشات وحدة الأجور — مبنيّة من مكوّنات ``ui2/`` حصراً.

كل شاشة ``QWidget`` تستدعي ``programme.payroll.repository`` (التخزين) و
طبقة النطاق ``programme.payroll.lignes`` / ``config_loader`` فقط — لا
تستدعي ``programme.payroll.calc`` مباشرةً (وحدها ``lignes`` تستدعي
المحرّك)، و**لا SQL ولا منطق حساب داخل هذه الحزمة**. لا استيراد من
``ui/`` القديمة.

الشاشات:
    companies.py   — الزبناء (جدول + إضافة/تعديل) — مبنيّة، غير معروضة
                     حالياً (تُفعَّل بعلمٍ في الإعدادات لاحقاً)
    bulletin.py    — توليد الكشف: منتقي الزبون + أسطر ديناميكية +
                     ‎[A]…[E]‎ + المناطق Z1..Z4
"""
