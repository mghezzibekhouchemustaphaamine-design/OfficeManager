"""شاشات وحدة الأجور — مبنيّة من مكوّنات ``ui2/`` حصراً.

كل شاشة ``QWidget`` تستدعي ``programme.payroll.repository`` (التخزين) و
``programme.payroll.calc`` / ``config_loader`` (الحساب) فقط — **لا SQL
ولا منطق حساب داخل هذه الحزمة**. لا استيراد من ``ui/`` القديمة.

الشاشات الخمس:
    companies.py   — الشركات (جدول + إضافة/تعديل)
    convention.py  — معاملات الاتفاقية (شارة لكل حقل + زرّ تأكيد)
    catalogue.py   — كتالوج الرُبريكات لكل شركة (قفز بصري للمنطقة)
    employees.py   — العمّال (جدول + إضافة/تعديل)
    bulletin.py    — توليد الكشف ([A]…[E] + المناطق Z1..Z4)
"""
